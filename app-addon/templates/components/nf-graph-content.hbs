<rect x=0 y=0 class="background" {{bind-attr width=width height=height}}></rect>

{{#if graph.hasRendered}}
  {{#if graph.hasData}}
    {{#if graph.showLanes}}
      <g class="nf-grid-lanes">
        {{#each lane in gridLanes}}
          <rect {{bind-attr x=lane.x y=lane.y width=width height=lane.height }}></rect>
        {{/each}}
      </g>

      {{#if graph.showXAxis}}
        <g class="nf-grid-frets">
          {{#each tick in graph.xAxis.ticks}}
            <line {{bind-attr x1=tick.x y1="0" x2=tick.x y2=height}}></line>
          {{/each}}
        </g>
      {{/if}}
    {{/if}}
  {{/if}}

  {{#unless graph.hasData}}
    <text x=0 y=0>No data</text>
  {{/unless}}
{{/if}}

{{yield}}